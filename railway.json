{
  "build": {
    "builder": "NIXPACKS",
    "config": {
      "installCommand": "pip install --no-cache-dir -r requirements.txt",
      "buildCommand": "apt-get update && apt-get install -y libmagic1 && python -m pip install --upgrade pip setuptools wheel"
    }
  },
  "deploy": {
    "startCommand": "python app.py"
  }
}
